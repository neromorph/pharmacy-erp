# Task 7 Report — Restyle Finance, Compliance, Master Data, System

Status: DONE_WITH_CONCERNS

## What was done (per page)

All pages restyled from inline styles to Tailwind v4 + shadcn/ui + lucide,
presentation-only. Server actions, fetches, permission gates, and form field
names unchanged.

- **finance/payables/page.tsx** — full shadcn Table, sticky `bg-slate-50`
  header, rows `h-10`, money cells `text-right tabular-nums`, status Badge
  (UNPAID outline / PARTIAL secondary / PAID default / OVERDUE destructive),
  credit chip `bg-primary` pill. Payout form moved into a shadcn `Dialog`
  (brief requirement) via new **payout-dialog.tsx** client component; keeps
  `postPayout` server-action contract (`accounts_payable_id`, `amount`,
  `method`, `notes`, hidden input, min/max/required).
- **finance/payables/aging-cards.tsx** — 5 bucket Cards (CURRENT, 1-30,
  31-60, 61-90, 90+), colored `borderTop`, `CardContent` grid `gap-3
  sm:grid-cols-2 lg:grid-cols-5`.
- **finance/payables/aging-csv-button.tsx** — Button `outline size="sm"`,
  download logic unchanged.
- **reports/sipnap/page.tsx** — tab bar as `Button render={<Link/>}`
  (active=default, inactive=outline), month/year `Input` with labels, Load
  Button, Missing Data + Hard-Block tables via shadcn Table, summary rows,
  Download via `DownloadButton`. Role gate, RPC fetch, pagination links
  unchanged.
- **reports/sipnap/download-button.tsx** — Button, busy/error state same.
- **reports/sipnap/download-link.tsx** — Button outline sm, same
  getStoredExport wiring.
- **reports/sipnap/history.tsx** — shadcn Table, hash `text-xs text-slate-500`.
- **doctors/page.tsx** — new-doctor form in Card (Label+Input grid gap-1.5),
  edit/remove inside native `<details>`, Remove Button `variant="destructive"`,
  Back Button outline. Role gate (`isOwner`) unchanged.
- **patients/page.tsx** — same treatment; BPJS/NIK fields, IHS note, role
  gates (canEdit/isOwner) unchanged.
- **settings/page.tsx** — OWNER gate unchanged, h1 + `SettingsForm`,
  `max-w-[560px] space-y-6`.
- **settings/settings-form.tsx** — sections split into Cards
  (Identity/Logo/Contact/Licenses/Receipt/SATUSEHAT), `Label`+`Input` grids,
  `Textarea` for footer, Test-connection Button outline, Save Button default,
  message banner green/red. All refs, ids, defaultValues, handlers unchanged.

**New file**: `apps/web/app/(app)/finance/payables/payout-dialog.tsx`
required by the brief ("payout form in a Dialog") — the payout form was
previously inline per row; Dialog needs a client component.

No lib/*, no actions.ts, no receipts page touched. No `.ts` client logic
changed (state, fetches, redirects byte-identical).

## Commands + output

- `cd apps/web && npx tsc --noEmit` → exit 0, clean.
- `cd apps/web && npx vitest run` → 12 files, 85/85 passed.
- `cd apps/web && npx next build` → green; route table intact (29 routes,
  same as baseline; no receipt (`/receipts/[saleId]`) changes).
- `pnpm run lint` → all 3 workspaces Done.
- Pre-commit hook (lint+build+audit+trivy) → passed in the running commit;
  `pnpm audit` 1 moderate (allowed, hook blocks only HIGH), trivy 0.

## Verification

See above. The external Sentry session and this task do not interact
functionally; tsc/build/lint verify both coexist.

## Concerns

1. **Commit attribution (main):** The concurrent Sentry session ran
   `git add -A` and absorbed my 12 staged files into its own commit
   `5774621` (message "fix(root): run web and api dev in parallel") —
   my full Task 7 diff is inside that commit, and the commit contains
   NOTHING else (verified `--name-only`: exactly my 12 files). Because the
   sibling session is actively committing on the same branch, I did not
   amend/rebuild the commit to avoid history race; I left the tree clean.
   A `style(web): restyle ...` message for those 12 files would be
   preferable; a follow-up cosmetic commit could `git rebase -i` to
   reword 577c1, but only when external session is idle.
2. `apps/web/next-env.d.ts` flip-flop (auto-regenerated by `next build`)
   restored to HEAD to avoid noise.
3. `paymentMethods` const moved from page into `payout-dialog.tsx` — no duplication remains.
4. Residual risk: low. Dialog payout flow not exercised in a browser after
   moving (the Pay button now opens a dialog rather than inline form).
   Server action contract preserved; suggest a manual E2E click during
   deploy verification.